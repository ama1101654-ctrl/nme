[pytest]
testpaths = tests
python_files = test_*.py
markers =
	e2e: end-to-end trade lifecycle regression tests
	browser: browser-level smoke and regression tests
	security: authentication, refresh, session ownership, rate limit, and CORS regression tests
	smoke: API availability and OpenAPI smoke tests
