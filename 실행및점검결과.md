# NME Trading Platform 실행 및 오류 점검 결과

## 1. 이미지 로드 및 실행 절차
1. PowerShell에서 아래 명령어로 이미지 로드
   ```powershell
   docker load -i "C:\Users\mhc11\Desktop\Nonferrous Metals Exchange\nme-trading-platform-ver1.tar"
   ```
2. 컨테이너 실행
   ```powershell
   docker run -d -p 3000:3000 nme-trading-platform:latest
   ```
3. 실행 확인
   ```powershell
   docker ps
   docker logs <컨테이너ID>
   ```
   - 정상적으로 실행되면 로그에 다음과 같이 표시됨:
     - Local: http://localhost:3000
     - ✓ Ready in ...ms

## 2. 오류 점검 결과
- 이미지 로드 및 컨테이너 실행 모두 정상
- Next.js 서버가 정상적으로 기동됨
- http://localhost:3000 에서 서비스 확인 가능

## 3. 문제 발생 시 조치
- 이미지 파일 경로 및 이름 확인
- Docker Desktop 실행 여부 확인
- 포트 충돌 시 다른 포트로 변경 가능
- 로그에서 오류 메시지 확인 후 대응

## 4. STEP 50 브라우저 E2E 회귀 검증
- Playwright 기반 browser smoke test를 추가하여 실제 브라우저에서 로그인 화면, 기본 사용자 진입, Market, 거래 이력 화면을 순차 검증함
- 브라우저 콘솔 오류, pageerror, API requestfailed, API 4xx/5xx 응답을 수집해 모두 0건으로 확인함
- backend 8000, frontend 5173을 실제로 기동한 뒤 `python -m pytest -q` 실행 결과 `9 passed`
- 기존 앱의 schema, JWT, API 동작은 변경하지 않음

## 5. STEP 51 Browser Trade Lifecycle E2E
- BUYER / SELLER 브라우저 context를 분리한 거래 lifecycle E2E test를 추가함
- FastAPI test instance + test SQLite DB 조합으로 browser mutation이 실개발 DB `nme.db`에 닿지 않도록 구성함
- Market → Deal 생성 → Deal 승인 → Order 생성 → Order 완료 → Completion → History → Dashboard를 하나의 browser flow로 검증하도록 설계함
- Browser console, page error, request failed, HTTP 4xx/5xx를 함께 수집하는 체계를 유지함
- Browser screenshot 저장 경로를 테스트 실행 시 생성하도록 구성함
- 실제 검증 결과: BUYER login PASS, SELLER login PASS, Market PASS, Deal create PASS, Deal approve PASS, Order create PASS, Order complete PASS, Completion PASS, History PASS, Dashboard PASS
- Browser monitoring: Console errors 0, Page errors 0, Request failed 0, Unexpected 4xx 0, Unexpected 5xx 0
- Browser tests: `python -m pytest -q tests/browser -m browser` -> `2 passed`
- Full pytest: `python -m pytest -q` -> `10 passed`
- Frontend build: `npm run build` 성공
- 실제 `nme.db` 전후 count: users 8 / products 4 / deals 23 / orders 16 / auth_sessions 82

## 6. STEP 52 개발환경 실행 표준화 및 Local Startup 안정화
- 프로젝트 루트의 `start_nme.bat`를 표준 시작점으로 정리함
- Backend 표준 주소는 `http://127.0.0.1:8000` 으로 고정함
- Frontend 표준 주소는 `http://127.0.0.1:5173` 으로 고정함
- Backend 실행 표준은 `python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload --no-proxy-headers` 임을 문서화함
- Frontend 실행 표준은 `npm run dev -- --host 127.0.0.1 --port 5173` 임을 문서화함
- 8000/5173 포트 충돌은 재시작으로 덮어쓰지 않고 명확한 오류로 구분하도록 정리함
- 점검 명령은 `/health`, `/market`, `/docs`, `/openapi.json` 순으로 고정함
- Browser E2E와 build 결과는 STEP 51의 통과 상태를 기준으로 유지함

## 7. STEP 53 NME Local Development Startup 검증 자동화
- 신규 비즈니스 기능 없이 개발환경 진단 전용 체계를 추가함
- 프로젝트 루트에 `check_nme.bat` + `check_nme.ps1`를 추가해 START/ CHECK 역할을 분리함

### 7.1 진단 스크립트 결과
- `start_nme.bat` 실행 전 `check_nme.bat`:
   - Backend/Frontend 미기동 상태를 `NOT RUNNING`으로 명확히 표시
   - `/health`, `/market`, `/docs`, `/openapi.json`, Frontend HTTP는 `FAIL`로 표시
   - 최종 판정 `RESULT: NOT READY`
- `start_nme.bat` 실행 후 `check_nme.bat`:
   - 8000/5173 `RUNNING/REUSE` 감지
   - Backend/Frontend 및 API 엔드포인트 전부 `PASS`
   - CORS 정책 및 VITE_API_URL 정책 `PASS`
   - 최종 판정 `RESULT: READY`

### 7.2 Backend/Frontend/API 결과
- Backend URL: `http://127.0.0.1:8000`
- Frontend URL: `http://127.0.0.1:5173`
- `/health`: 200
- `/market`: 200
- `/docs`: 200
- `/openapi.json`: 200
- Frontend root: 200

### 7.3 Port / API URL / CORS 결과
- Port 8000: PASS (`RUNNING/REUSE`)
- Port 5173: PASS (`RUNNING/REUSE`)
- API URL: PASS (`VITE_API_URL` 정책 `http://127.0.0.1:8000`)
- CORS: PASS (`http://127.0.0.1:5173` 허용, `http://evil.example.com` 비허용, wildcard 미사용)

### 7.4 Browser / pytest / build 결과
- Browser tests: `python -m pytest -q tests/browser -m browser` -> `2 passed`
- Browser smoke: PASS
- Browser trade flow: PASS
- Browser Console: 0 errors (기존 browser test assertion 유지)
- Browser Network: Request failed 0, Unexpected 4xx/5xx 0 (기존 browser test assertion 유지)
- Full pytest: `python -m pytest -q` -> `10 passed`
- Frontend build: `npm run build` -> PASS

### 7.5 DB 보호 결과
- `nme_backend/nme.db` 읽기 전용 count 확인:
   - users 8
   - products 4
   - deals 23
   - orders 16
   - auth_sessions 82
- STEP 52 기준 값과 동일하여 실개발 DB 변경 없음

### 7.6 오류 및 해결
- 초기 check 실행에서 NOT READY가 출력됨(서비스 미기동 상태)
- 해결: 표준 시작점 `start_nme.bat` 실행 후 check 재실행하여 READY 확인

### 7.7 최종 판정
- STEP 53 STATUS: COMPLETE
- 진단 체계 정상, 회귀 테스트 정상, DB 보호 정상

## 8. STEP 54 NME Quality Gate / 통합 사전검증 자동화
- `verify_nme.bat` + `verify_nme.ps1`를 추가해 START/CHECK/VERIFY를 분리함
- VERIFY는 `check_nme.bat`를 먼저 실행하고, 실패 시 `start_nme.bat` 정책으로 재시도 후 품질 검증을 계속함

### 8.1 실행 순서 및 결과
1. Environment check (`check_nme.bat`) PASS
2. Backend/Frontend + API (`/health`, `/market`, `/docs`, `/openapi.json`) PASS
3. CORS(allowed origin / evil origin) PASS
4. DB pre-count(read-only) PASS
5. Full pytest PASS (`10 passed, 47 warnings`)
6. Browser pytest PASS (`2 passed`)
7. Frontend build PASS
8. DB post-count(read-only) PASS
9. `NME QUALITY GATE: PASS`

### 8.2 Port / URL / 계약 보호
- Backend URL: `http://127.0.0.1:8000`
- Frontend URL: `http://127.0.0.1:5173`
- Port 8000/5173 재사용 정책 유지
- API contract/JWT/DB schema/business logic 변경 없음

### 8.3 Browser monitoring 결과
- Console errors: 0 (기존 assertion 유지)
- Page errors: 0 (기존 assertion 유지)
- Request failed: 0 (기존 assertion 유지)
- Unexpected 4xx/5xx: 0 (기존 assertion 유지, 의도된 404 예외 정책 유지)

### 8.4 실제 DB 보호 결과
- `nme_backend/nme.db` pre/post count 동일
   - users 8
   - products 4
   - deals 23
   - orders 16
   - auth_sessions 82
- 테스트 DB isolation 유지

### 8.5 오류 및 해결
- 초기 verify 스크립트에서 PowerShell 파서 오류와 frontend check(404) 이슈 발생
- 해결: verify 스크립트의 인코딩/인용부호/frontend HTML check를 수정하여 안정화

### 8.6 최종 판정
- STEP 54 STATUS: COMPLETE
- NME QUALITY GATE: PASS

## 9. STEP 55 NME CI Pre-flight 자동 품질검증 구축
- 기존 CI 설정 없음 확인 후 GitHub Actions workflow `.github/workflows/nme-ci.yml` 추가
- Trigger: `push`, `pull_request`
- Runner: `ubuntu-latest`
- Python 3.13 / Node 24 기준으로 품질 게이트 구성

### 9.1 Workflow 단계
1. checkout
2. Python setup
3. Node setup
4. backend dependency install
5. frontend dependency install (`npm ci`)
6. Playwright Chromium install
7. repo `nme.db` 사전 count(read-only)
8. isolated SQLite DB 기반 API smoke + CORS
9. full pytest
10. browser pytest
11. frontend build
12. repo `nme.db` 사후 count(read-only)
13. `NME QUALITY GATE: PASS/FAIL`

### 9.2 실측 검증 결과
- Local verify: `verify_nme.bat` -> PASS
- Full pytest: `10 passed, 47 warnings`
- Browser pytest: `2 passed`
- Frontend build: PASS
- CI-equivalent isolated smoke:
   - `/health` 200
   - `/market` 200
   - `/docs` 200
   - `/openapi.json` 200
   - allowed CORS origin `http://127.0.0.1:5173` PASS
   - denied origin `http://evil.example.com` PASS

### 9.3 DB isolation / repo DB 보호
- workflow smoke 단계는 `sqlite:///./nme_ci_smoke.db` 사용
- browser fixture는 기존 동적 port + test runtime 구조 유지
- 실제 `nme_backend/nme.db` 전후 count 동일:
   - users 8
   - products 4
   - deals 23
   - orders 16
   - auth_sessions 82

### 9.4 오류 및 해결
- 초기 로컬 CI smoke 재현에서 Windows 절대경로 기반 SQLite URL이 잘못되어 DB open 실패
- 해결: workflow 및 로컬 재현 방식을 `sqlite:///./nme_ci_smoke.db`로 단순화해 동일 동작 확인

### 9.5 한계
- 현재 workspace에는 `.git` 디렉터리와 remote 정보가 없어서 실제 GitHub Actions runner에서 workflow를 직접 실행하지는 못함
- 따라서 CI workflow 자체는 작성 완료, 로컬 동등 명령으로 실측 검증 완료 상태

### 9.6 최종 판정
- STEP 55 STATUS: PARTIAL
- 사유: CI workflow와 로컬 동등 검증은 완료했지만 실제 원격 CI runner 실행은 현재 환경에서 확인 불가

---
2025년 9월 22일, 실행 및 점검 자동 기록
