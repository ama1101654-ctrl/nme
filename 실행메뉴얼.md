# NME Trading Platform 실행 메뉴얼

## 1. 실행 환경
- 운영체제: Linux 기반 컨테이너 환경 (Windows에서는 Docker Desktop 필요)
- 필수 도구: Docker, Docker Compose

## 2. 실행 방법
1. Docker Desktop 또는 Docker Engine 설치
2. 터미널(명령 프롬프트 또는 PowerShell) 실행
3. 해당 폴더로 이동
   ```powershell
   cd "c:\Users\mhc11\Desktop\Nonferrous Metals Exchange\nme-trading-platform-ver1"
   ```
4. 이미지 로드 및 실행
   ```powershell
   docker load -i manifest.json
   docker run -d -p 3000:3000 nme-trading-platform:latest
   ```
5. 웹 브라우저에서 접속
   - 주소: http://localhost:3000

## 3. 실행 확인
- 정상적으로 실행되면 웹 브라우저에서 NME Trading Platform 화면 확인 가능
- 로그 및 오류는 Docker Desktop의 컨테이너 로그에서 확인

## 4. 문제 해결
- Docker가 실행 중인지 확인
- 포트 충돌 시 다른 포트로 변경 가능
- 이미지가 정상적으로 로드되지 않으면 manifest.json, blobs/sha256 경로 확인

---
2025년 9월 22일, 실행 메뉴얼 자동 생성
